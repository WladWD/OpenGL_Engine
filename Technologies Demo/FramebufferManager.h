#include "MainHeader.h"
#include <vector>
#pragma once

namespace Resources
{
	class FramebufferManager
	{
		//////////////////////////////////////////////////////////////////////////////
		GLuint framebuffer;
		std::vector<GLuint> attachTexture;
		GLuint attachDepth;
		//////////////////////////////////////////////////////////////////////////////
		//////////////////////////////////////////////////////////////////////////////
	public:
		FramebufferManager(void);
		~FramebufferManager();
		//////////////////////////////////////////////////////////////////////////////
		void initFramebuffer(uint32_t textureCount, const GLuint *textures);
		void initDepthTexture(GLuint depth);
		//////////////////////////////////////////////////////////////////////////////
		GLuint getFramebuffer(void);
		const GLuint * getFramebufferTextures(void);
		GLuint getFramebufferTexture(uint32_t idx);
		GLuint getFramebufferDepth(void);
		//////////////////////////////////////////////////////////////////////////////
	};
}

