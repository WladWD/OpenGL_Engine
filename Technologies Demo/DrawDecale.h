#include "ShaderDecale.h"
#include "DecaleManager.h"
#include "GBufferData.h"

#pragma once
namespace Decale
{
	class DrawDecale
	{
		//////////////////////////////////////////////////////////////////////////////////////
		Shader::ShaderDecale *mShader;
		DecaleManager *mDecaleManager;
		//////////////////////////////////////////////////////////////////////////////////////
		//////////////////////////////////////////////////////////////////////////////////////
		void Init(void);
	public:
		DrawDecale(Resource::LoadTexture *mTextureLoader, std::string mDecaleImage);
		~DrawDecale();
		//////////////////////////////////////////////////////////////////////////////////////
		void AppendDecale(MCamera::Camera *mCamera);
		void Draw(MCamera::Camera * mCamera, DeferredRendering::GBufferData mGBufferData);
		//////////////////////////////////////////////////////////////////////////////////////
	};
}

