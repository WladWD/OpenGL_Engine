#include "GBufferData.h"

namespace Render 
{
	class FinalFrame 
	{
		////////////////////////////////////////////////////////////////////////////////////////////////////////////
		uint32_t mDimX;
		uint32_t mDimY;
		////////////////////////////////////////////////////////////////////////////////////////////////////////////
		glm::mat4 mProjView;
		glm::mat4 mView;
		////////////////////////////////////////////////////////////////////////////////////////////////////////////
		GLuint mImage;
		////////////////////////////////////////////////////////////////////////////////////////////////////////////
		GLuint mDepth;
		////////////////////////////////////////////////////////////////////////////////////////////////////////////
		GLuint mFramebuffer;
		////////////////////////////////////////////////////////////////////////////////////////////////////////////
		void InitFramebuffer(void);
	public:
		FinalFrame(GLuint mDepth, uint32_t mDimX, uint32_t mDimY);
		~FinalFrame(void);
		////////////////////////////////////////////////////////////////////////////////////////////////////////////
		void Resize(GLuint mDepth, uint32_t mDimX, uint32_t mDimY);
		////////////////////////////////////////////////////////////////////////////////////////////////////////////
		void BingFramebuffer(void);
		////////////////////////////////////////////////////////////////////////////////////////////////////////////
		GLuint GetCurrentFramebuffer(void);
		////////////////////////////////////////////////////////////////////////////////////////////////////////////
	};
}
//https://github.com/SaschaWillems/VulkanSponza